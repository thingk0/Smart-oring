/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.16 ./public/models/charger.glb -t -T -I --shadows 
Files: ./public/models/charger.glb [12.23KB] > C:\Users\SSAFY\Documents\GitLab\S10P31S109\frontend\charger-transformed.glb [1.92KB] (84%)
*/

import * as THREE from 'three';
import React, { useRef, useMemo, useContext, createContext } from 'react';
import { useGLTF, Merged } from '@react-three/drei';
import { GLTF } from 'three-stdlib';

type GLTFResult = GLTF & {
  nodes: {
    Cube: THREE.Mesh;
  };
  materials: {
    ['Material.002']: THREE.MeshStandardMaterial;
  };
};

type ContextType = Record<
  string,
  React.ForwardRefExoticComponent<JSX.IntrinsicElements['mesh']>
>;

const context = createContext({} as ContextType);
export function Instances({
  children,
  ...props
}: JSX.IntrinsicElements['group']) {
  const { nodes } = useGLTF('/models/charger-transformed.glb') as GLTFResult;
  const instances = useMemo(
    () => ({
      Cube: nodes.Cube,
    }),
    [nodes]
  );
  return (
    <Merged meshes={instances} {...props} castShadow>
      {(instances: ContextType) => (
        <context.Provider value={instances} children={children} />
      )}
    </Merged>
  );
}

export function Model(props: JSX.IntrinsicElements['group']) {
  const instances = useContext(context);
  return (
    <group {...props} dispose={null}>
      <instances.Cube scale={0.45} />
    </group>
  );
}

useGLTF.preload('/models/charger-transformed.glb');
